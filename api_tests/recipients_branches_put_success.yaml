# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /recipients/branches_put for http method type PUT in artillery framework
# RoostTestHash=63322ffb26
# 
# 

# ********RoostGPT********
config:
  target: "{{ processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "recipients_branches_put_success.csv"
    fields: ["cocNumber", "cocBranchNumber", "city", "tradeName", "phone", "email"]
    skipHeader: true
    delimiter: "^|^"
  headers:
    Authorization: "Bearer {{processEnvironment.AUTH_TOKEN}}"
    Content-Type: "application/json"

scenarios:
  - name: "Testing the Recipient Branch API"
    flow:
      - log: "Making a POST request to /recipients/branches"
      - post:
          url: "/recipients/branches"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasHeader: "api-version"
            - hasHeader: "location"
            - hasProperty: 'id'
            - hasProperty: 'email'
            - hasProperty: 'phone'
            - hasProperty: 'cocNumber'
            - hasProperty: 'cocBranchNumber'
            - hasProperty: 'tradeName'
            - hasProperty: 'tradeNames'
            - hasProperty: 'eoriNumber'
            - hasProperty: 'deregistrationDate'
            - hasProperty: 'createdAt'
            - hasProperty: 'updatedAt'
      - think: 5
