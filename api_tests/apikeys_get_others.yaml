# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /apikeys_get for http method type GET in artillery framework
# RoostTestHash=9e57a3b0dd
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "apikeys_get_others.csv"
    fields: 
      - "name"
      - "apikey"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Sending a GET request with invalid URL"
      - get:
          url: "/invalid_url"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending a GET request without authorization"
      - get:
          url: "/apikeys"
          expect:
            - statusCode: 403
      - think: 1

      - log: "Sending a GET request with incorrect authorization"
      - get:
          url: "/apikeys"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending a GET request with valid authorization but expecting incorrect response code"
      - get:
          url: "/apikeys"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
      - think: 1
