# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /recipients/branches/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=1ce4bf6e3c
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "recipients_branches_id_delete_others.csv"
    fields: ["id"]
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Sending a GET request with an invalid ID"
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Sending a GET request without an ID"
      - get:
          url: "/recipients/branches/"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Sending a GET request with an ID that is not UUID"
      - get:
          url: "/recipients/branches/random_string"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Sending a GET request without authentication"
      - get:
          url: "/recipients/branches/{{ id }}"
          expect:
            - statusCode: 401
      - think: 1
