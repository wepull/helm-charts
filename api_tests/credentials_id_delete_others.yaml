# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=f1fc1d06f7
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "credentials_id_delete_others.csv"
    fields: ["id"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://dev.api.example.com"
    prod:
      target: "http://api.example.com"
scenarios:
  - name: "Non-success API response tests"
    flow:
      - log: "Making a DELETE request with invalid id"
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Making a DELETE request without authorization"
      - delete:
          url: "/credentials/{{ id }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Making a DELETE request with expired authorization"
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.EXPIRED_AUTH_TOKEN }}"
          expect:
            - statusCode: 401
      - think: 1
