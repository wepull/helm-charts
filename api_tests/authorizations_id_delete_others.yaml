# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=cb128291e8
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        rejectUnauthorized: false
        timeout: 10000

scenarios:
  - name: "Testing non-successful API requests"
    flow:
      - log: "Attempting to delete authorization with invalid ID"
      - delete: 
          url: "/authorizations/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Attempting to delete authorization without authentication"
      - delete: 
          url: "/authorizations/{{ id }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Attempting to delete authorization with invalid URL"
      - delete: 
          url: "/invalid_url/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Attempting to delete authorization with missing ID parameter"
      - delete: 
          url: "/authorizations/"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
