# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /recipients/branches/{id}_get for http method type GET in artillery framework
# RoostTestHash=061355b093
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    - expect: statusCode
    - expect: hasProperty
    - expect: equals
    - expect: hasHeader
    - expect: headerEquals
    - expect: matchesRegexp
  plugins:
    expect: {}
  payload:
    path: "recipients_branches_id_get_success.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  environments:
    staging:
      target: "{{ $processEnvironment.API_HOST }}"

scenarios:
  - name: "Validating Successful API responses"
    flow:
      - log: "Starting Scenario for GET /recipients/branches/{id}"
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            "x-api-key": "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "cocBranchNumber"
            - hasProperty: "cocNumber"
            - hasProperty: "createdAt"
            - hasProperty: "deregistrationDate"
            - hasProperty: "eoriNumber"
            - hasProperty: "id"
            - hasProperty: "tradeName"
            - hasProperty: "tradeNames"
            - hasProperty: "updatedAt"
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "1.0.0"]
          capture:
            - json: "$.id"
              as: "id"
      - think: 1
      - log: "Finished Scenario for GET /recipients/branches/{id}"
