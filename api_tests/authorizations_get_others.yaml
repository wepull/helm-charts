# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /authorizations_get for http method type GET in artillery framework
# RoostTestHash=3b64f22044
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
    delimiter: "^|^"
  phases:
    - duration: 10
      arrivalRate: 10

scenarios:
  - name: "Non-success API responses test"
    flow:
      - log: "Sending invalid GET request to /authorizations"
      - get:
          url: "/authorizations"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Sending valid GET request to invalid URL /invalid"
      - get:
          url: "/invalid"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Sending GET request to /authorizations without required headers"
      - get:
          url: "/authorizations"
          expect:
            - statusCode: 401
            - hasHeader: "www-authenticate"
      - think: 1
      - log: "Sending GET request to /authorizations with incorrect ApiKeyAuth"
      - get:
          url: "/authorizations"
          headers:
            ApiKeyAuth: "incorrect_token"
          expect:
            - statusCode: 403
      - think: 1
