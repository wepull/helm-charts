# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "login_post_others.csv"
    fields:
      - "email"
      - "otp"
      - "client"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://dev.example.com"
      variables:
        AUTH_TOKEN: "devtoken"
scenarios:
  - name: "Non-successful API requests"
    flow:
      - log: "Attempting to login with invalid credentials"
      - post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals:
                message: "Invalid credentials"
      - think: 1
      - log: "Attempting to access a non-existent endpoint"
      - get:
          url: "/nonExistentEndpoint"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals:
                message: "Endpoint not found"
      - think: 1
      - log: "Attempting to access an endpoint without authorization"
      - get:
          url: "/login"
          expect:
            - statusCode: 403
            - hasProperty: "message"
            - equals:
                message: "Authorization required"
      - think: 1
