# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /apikeys_post for http method type POST in artillery framework
# RoostTestHash=28a54f1579
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "apikeys_post_others.csv"
    fields: ["username", "password"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        timeout: 10
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Invalid auth token"
      - post:
          url: "/apikeys"
          headers:
            Authorization: "invalid_token"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Missing auth token"
      - post:
          url: "/apikeys"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Invalid request body"
      - post:
          url: "/apikeys"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "test"
            password: "test"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Non-existent API endpoint"
      - post:
          url: "/nonexistent"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Valid request but server error"
      - post:
          url: "/apikeys"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "test"
            password: "test"
          expect:
            - statusCode: 500
      - think: 1
