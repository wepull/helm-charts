# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /apikeys_get for http method type GET in artillery framework
# RoostTestHash=9e57a3b0dd
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "apikeys_get_success.csv"
    fields:
      - "apiKey"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "API key validation"
    flow:
      - log: "Starting API key validation scenario"
      - get:
          url: "/apikeys"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "apikeys"
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
      - think: 1
      - log: "Successfully validated API key"
