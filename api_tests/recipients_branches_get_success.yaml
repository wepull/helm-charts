# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /recipients/branches_get for http method type GET in artillery framework
# RoostTestHash=74a40da00e
# 
# 

# ********RoostGPT********
config:
  target: "default-target"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "recipients_branches_get_success.csv"
    fields:
      - "coc-number"
      - "coc-branch-number"
      - "trade-name"
      - "size"
      - "offset"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
  processor: "./myScript.js"
scenarios:
  - name: "Test Scenario - Successful API requests"
    beforeScenario: "setHeaders"
    flow:
      - log: "Starting test for GET /recipients/branches"
      - get:
          url: "/recipients/branches?coc-number={{ coc-number }}&coc-branch-number={{ coc-branch-number }}&trade-name={{ trade-name }}&size={{ size }}&offset={{ offset }}"
          headers:
            ApiKeyAuth: "{{ auth }}"
          expect:
            - statusCode: 200
            - hasProperty: "branches"
            - hasProperty: "total"
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
      - think: 1
