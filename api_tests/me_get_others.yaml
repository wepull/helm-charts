# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /me_get for http method type GET in artillery framework
# RoostTestHash=ae6b8ce5ab
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "me_get_others.csv"
    fields:
      - "field1"
      - "field2"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Sending GET request with invalid URL"
      - get:
          url: "/invalid_url"
          headers:
            ApiKeyAuth: "InvalidApiKey"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending GET request without authentication"
      - get:
          url: "/me"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending GET request with invalid authentication"
      - get:
          url: "/me"
          headers:
            ApiKeyAuth: "InvalidApiKey"
          expect:
            - statusCode: 403
      - think: 1

      - log: "Sending GET request with valid authentication but invalid payload"
      - get:
          url: "/me"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            field1: "invalid_field1"
            field2: "invalid_field2"
          expect:
            - statusCode: 400
      - think: 1
