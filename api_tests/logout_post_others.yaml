# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /logout_post for http method type POST in artillery framework
# RoostTestHash=64d33906e5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "logout_post_others.csv"
    fields: ["username", "password"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://dev.example.com"
      $processEnvironment:
        AUTH_TOKEN: "devAuthToken"

scenarios:
  - name: "Logout API Failure Cases"
    flow:
      - log: "Sending invalid GET request to /logout"
      - get:
          url: "/logout"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 405
      - think: 1
      - log: "Sending invalid POST request to /logout"
      - post:
          url: "/logout"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            username: "{{ username }}"
            password: "{{ password }}"
          expect:
            - statusCode: 405
      - think: 1
      - log: "Sending DELETE request to /logout with invalid token"
      - delete:
          url: "/logout"
          headers:
            ApiKeyAuth: "invalid_token"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Sending DELETE request to /logout with valid token"
      - delete:
          url: "/logout"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 204
      - think: 1
      - log: "Sending DELETE request to /logout again with same valid token"
      - delete:
          url: "/logout"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Sending DELETE request to incorrect URL"
      - delete:
          url: "/logut"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
