# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /credentials_get for http method type GET in artillery framework
# RoostTestHash=c5011f3c64
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "credentials_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Sending invalid GET request to /credentials"
      - get:
          url: "/credentials?size=5000"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals: ["message", "Size parameter value is too large"]
      - think: 1
      - log: "Sending GET request to non-existing endpoint"
      - get:
          url: "/invalid_endpoint"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: ["message", "Endpoint not found"]
      - think: 1
      - log: "Sending GET request to /credentials without authorization"
      - get:
          url: "/credentials"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: ["message", "Authorization required"]
      - think: 1
      - log: "Sending GET request to /credentials with invalid authorization"
      - get:
          url: "/credentials"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 403
            - hasProperty: "message"
            - equals: ["message", "Invalid authorization token"]
      - think: 1
