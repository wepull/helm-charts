# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /grants_get for http method type GET in artillery framework
# RoostTestHash=692c6b853f
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "grants_get_others.csv"
    fields:
      - "id"
      - "name"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Non-successful API responses"
    flow:
      - log: "Sending a GET request with incorrect URL"
      - get:
          url: "/invalid_url"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending a GET request without authorization header"
      - get:
          url: "/grants"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending a GET request with invalid authorization token"
      - get:
          url: "/grants"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 403
      - think: 1

      - log: "Sending a GET request with valid authorization token but non-existing grant"
      - get:
          url: "/grants/{{ id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1
