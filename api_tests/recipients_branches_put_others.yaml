# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /recipients/branches_put for http method type PUT in artillery framework
# RoostTestHash=63322ffb26
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 120
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "recipients_branches_put_others.csv"
    fields:
      - "cocNumber"
      - "cocBranchNumber"
      - "city"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "Test non-successful API requests"
    flow:
      - log: "Sending incorrect POST request to /recipients/branches"
      - post:
          url: "/recipients/branches"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 5
      - log: "Sending incorrect POST request to an invalid URL"
      - post:
          url: "/invalid/url"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 404
      - think: 5
