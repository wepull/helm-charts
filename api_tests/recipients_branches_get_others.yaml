# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /recipients/branches_get for http method type GET in artillery framework
# RoostTestHash=74a40da00e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "recipients_branches_get_others.csv"
    fields:
      - "coc-number"
      - "coc-branch-number"
      - "trade-name"
      - "size"
      - "offset"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        rejectUnauthorized: false
        headers:
          Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Sending GET request with invalid coc-number"
      - get:
          url: "/recipients/branches?coc-number=123456789"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Sending GET request with invalid coc-branch-number"
      - get:
          url: "/recipients/branches?coc-branch-number=1234567890123"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Sending GET request with invalid trade-name"
      - get:
          url: "/recipients/branches?trade-name=invalidtradename"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Sending GET request with invalid size"
      - get:
          url: "/recipients/branches?size=51"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Sending GET request with invalid offset"
      - get:
          url: "/recipients/branches?offset=-1"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Sending GET request without authorization token"
      - get:
          url: "/recipients/branches"
          headers:
            Authorization: "Bearer invalidtoken"
          expect:
            - statusCode: 401
      - think: 1
