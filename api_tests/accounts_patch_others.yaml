# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /accounts_patch for http method type PATCH in artillery framework
# RoostTestHash=2758affb7e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "accounts_patch_others.csv"
    fields: ["email"]
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
scenarios:
  - name: "Test for non-successful API requests"
    flow:
      - log: "Sending invalid login request with missing email"
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: ""
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals: 
                message: "Email is required"
      - think: 2

      - log: "Sending invalid login request with incorrect email format"
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "invalid_email"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals: 
                message: "Email format is incorrect"
      - think: 2

      - log: "Sending request to non-existent URL"
      - post:
          url: "/non_existent"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
      - think: 2
