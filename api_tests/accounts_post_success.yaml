# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "accounts_post_success.csv"
    fields:
      - "email"
    skipHeader: true
    delimiter: "^|^"
  environments:
    default:
      target: "{{ $processEnvironment.API_HOST }}"
scenarios:
  - name: "Accounts API Tests"
    flow:
      - log: "Posting to /accounts with valid inputs"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "{{$processEnvironment.COMMIT_HASH}}"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            email: "{{ email }}"
      - expect:
          - statusCode: 201
          - hasHeader: "api-version"
          - hasHeader: "commit-hash"
      - headerEquals: 
          api-version: "1.0.0"
      - matchesRegexp:
          commit-hash: "^[a-z0-9]{7}$"
      - think: 1
