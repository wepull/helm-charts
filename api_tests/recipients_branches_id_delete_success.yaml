# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /recipients/branches/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=1ce4bf6e3c
# 
# 

# ********RoostGPT********
config:
  target: "${processEnvironment.API_HOST}"
  plugins:
    expect: {}
  payload:
    path: "recipients_branches_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "Successful API Responses Scenario"
    flow:
      - log: "Starting test for DELETE /recipients/branches/{id}"
      - delete:
          url: "/recipients/branches/${id}"
          headers:
            ApiKeyAuth: "${processEnvironment.AUTH_TOKEN}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: [{"api-version": "1.0.0"}]
      - think: 1
      - log: "Starting test for GET /recipients/branches/{id}"
      - get:
          url: "/recipients/branches/${id}"
          headers:
            ApiKeyAuth: "${processEnvironment.AUTH_TOKEN}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: [{"api-version": "1.0.0"}]
      - think: 1
