# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=c02d3561ab
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "credentials_id_get_success.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Test Scenario: One credential from my Wallet"
    flow:
      - log: "Starting test for GET /credentials/{id}"
      - get:
          url: "/credentials/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
            - statusCode: 200
      - expect:
            - hasHeader: "api-version"
      - expect:
            - hasProperty: "id"
      - expect:
            - hasProperty: "format"
      - expect:
            - hasProperty: "credential"
      - expect:
            - hasProperty: "status"
      - expect:
            - hasProperty: "statusUpdatedAt"
      - expect:
            - hasProperty: "createdAt"
      - think: 1
