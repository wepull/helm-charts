# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /me_patch for http method type PATCH in artillery framework
# RoostTestHash=6dd436357d
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "me_patch_others.csv"
    fields: ["name", "image", "settings"]
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Sending PATCH request with invalid payload"
      - patch:
          url: "/me"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
      - expect:
            - statusCode: 400
      - think: 1
      - log: "Sending PATCH request without authorization"
      - patch:
          url: "/me"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
      - expect:
            - statusCode: 401
      - think: 1
      - log: "Sending PATCH request to non-existent URL"
      - patch:
          url: "/nonexistent"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
      - expect:
            - statusCode: 404
      - think: 1
