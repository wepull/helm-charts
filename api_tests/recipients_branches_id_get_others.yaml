# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /recipients/branches/{id}_get for http method type GET in artillery framework
# RoostTestHash=061355b093
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "recipients_branches_id_get_others.csv"
    fields: ["id"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://dev.api.example.com"
      security:
        - ApiKeyAuth: []
        - in: header
          name: "X-Api-Key"
          schema:
            type: string
          value: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - flow:
    - log: "Getting recipient branch with invalid id"
    - get:
        url: "/recipients/branches/{{ id }}"
        headers:
          X-Api-Key: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
          - hasProperty: "error"
          - equals: ["error", "Recipient branch not found"]
    - think: 1
    - log: "Getting recipient branch with missing id"
    - get:
        url: "/recipients/branches/"
        headers:
          X-Api-Key: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: "error"
          - equals: ["error", "Missing id"]
    - think: 1
    - log: "Getting recipient branch with invalid API key"
    - get:
        url: "/recipients/branches/{{ id }}"
        headers:
          X-Api-Key: "invalid"
        expect:
          - statusCode: 403
          - hasProperty: "error"
          - equals: ["error", "Invalid API key"]
    - think: 1
    - log: "Getting recipient branch with missing API key"
    - get:
        url: "/recipients/branches/{{ id }}"
        headers:
          X-Api-Key: ""
        expect:
          - statusCode: 401
          - hasProperty: "error"
          - equals: ["error", "Missing API key"]
    - think: 1
