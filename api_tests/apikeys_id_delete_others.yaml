# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /apikeys/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=4affc26394
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "apikeys_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Sending DELETE request with invalid API key"
      - delete:
          url: "/apikeys/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
            - headerEquals: ["content-type", "application/json"]
      - think: 1

      - log: "Sending DELETE request with valid API key but non-existent id"
      - delete:
          url: "/apikeys/nonexistentid"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
            - headerEquals: ["content-type", "application/json"]
      - think: 1

      - log: "Sending DELETE request with valid API key and id but incorrect method"
      - get:
          url: "/apikeys/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
            - hasHeader: "content-type"
            - headerEquals: ["content-type", "application/json"]
      - think: 1
