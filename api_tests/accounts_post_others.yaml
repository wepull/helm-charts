# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "accounts_post_others.csv"
    fields: ["email"]
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Success API Responses Scenario"
    flow:
      - log: "Starting Scenario - Testing POST /accounts with invalid email"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "1234567"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "errors"
      - capture:
          json: "$.errors"
          as: "error_message"
      - think: 1

      - log: "Starting Scenario - Testing POST /accounts with missing commit-hash"
      - post:
          url: "/accounts"
          json:
            email: "test@mail.com"
          expect:
            - statusCode: 400
            - hasProperty: "errors"
      - capture:
          json: "$.errors"
          as: "error_message"
      - think: 1

      - log: "Starting Scenario - Testing POST /accounts with incorrect URL"
      - post:
          url: "/account"
          headers:
            commit-hash: "1234567"
          json:
            email: "test@mail.com"
          expect:
            - statusCode: 404
      - capture:
          json: "$.errors"
          as: "error_message"
      - think: 1
